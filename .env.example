# Public values (exposed to client). Vercel env keys must use NUXT_PUBLIC_ prefix.
# Replace placeholders with your real keys in your local `.env` (do NOT commit real keys).
NUXT_PUBLIC_TMDB_API_KEY=your_tmdb_api_key_here
NUXT_PUBLIC_TMDB_IMAGE_BASE=https://image.tmdb.org/t/p
NUXT_PUBLIC_TMDB_BASE_URL=https://api.themoviedb.org/3

# Optional server-only fallback (only if you prefer server secret)
# TMDB_API_KEY=your_server_only_tmdb_api_key_here